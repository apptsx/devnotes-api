# Roadmap

* Refresh tokens with rotation.
* Server-side note encryption at rest (AES-GCM keyed by user secret).
* Webhooks on note create/update/delete.
* Rate limiting per user for auth endpoints.
* OpenTelemetry traces + structured JSON logs.